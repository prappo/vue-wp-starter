export function msg() {
    const chalk = require('chalk');
    const log = console.log;

    log(`
         Welcome to ${chalk.green('Vue.js')} ${chalk.blue('Wordpress')} Starter 
         A WordPress ${chalk.yellow('Vue.js')} starter plugin with required toolbelts 
`);


}